version: '3'
services:
        web:
                image: healthmedia
                build: .
                env_file:
                        - .env # This file is optional; defaults will be used if it does not exist
                ports:
                        - "8106:8106"
                volumes:
                        - ./instance:/instance
                restart: unless-stopped
